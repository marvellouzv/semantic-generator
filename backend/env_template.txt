# IMPORTANT:
# - Put your real key ONLY into backend/.env (do NOT commit it).
# - This file is a template and must NOT contain real secrets.

# Primary LLM configuration (OpenAI-compatible provider, e.g. OpenRouter)
OPENAI_API_KEY=sk-or-v1-your-openrouter-key-here
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-5.1
OPENAI_FALLBACK_MODEL=openai/gpt-5-mini

# Optional provider headers (recommended by OpenRouter)
# OPENROUTER_SITE_URL=https://your-site.example
# OPENROUTER_SITE_NAME=Semantic Generator

# Backward compatibility (optional):
# OPENROUTER_API_KEY=
# OPENROUTER_BASE_URL=
# OPENROUTER_MODEL=
# OPENROUTER_FALLBACK_MODEL=

# Request Configuration
REQUEST_TIMEOUT_SECONDS=300
USE_GPT5_ENSEMBLE=true

# Redis Caching
REDIS_URL=redis://localhost:6379
